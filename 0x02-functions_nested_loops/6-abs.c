#include "main.h"
/**
 * _abs(int) - computes the absolute value of an integer
 * @i: get absolute value of an integer
 *
 * Return: absolute value
 */
int _abs(int)
{
if (i < 0)
return (-i);
return (i);
}

